{
  "name": "restaurant-saas",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api":    "npm run dev --workspace=apps/api",
    "dev:socket": "npm run dev --workspace=apps/socket",
    "dev":        "npm run dev:api & npm run dev:socket"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1"
  }
}
